const heroRail = [
  {
    name: "ASUS TUF Gaming A15",
    image: "./assets/tech/cutout-laptop.png",
    alt: "Laptop product image on a dark background",
  },
  {
    name: "TP-Link Archer C6 Router",
    image: "./assets/tech/cutout-router.png",
    alt: "Router product image on a light background",
  },
  {
    name: "Seagate Barracuda SSD",
    image: "./assets/tech/cutout-ssd.png",
    alt: "SSD product image on a light background",
  },
];

const drops = [
  {
    label: "Featured laptop",
    title: "ASUS TUF Gaming A15",
    copy: "Performance-focused hardware presented as a clean studio hero that fits the storefront layout.",
    image: "./assets/tech/drop-laptop.jpg",
    alt: "Laptop banner with a dark premium background",
  },
  {
    label: "Featured networking",
    title: "TP-Link Archer C6 AC1200 Router",
    copy: "Dual-band wireless coverage with a product-only presentation that feels sharper and more premium.",
    image: "./assets/tech/drop-router.jpg",
    alt: "Router banner with a light studio background",
  },
  {
    label: "Featured printing",
    title: "Brother MFC-L2759DW Printer",
    copy: "A business-ready laser printer shown as a dedicated product hero instead of a borrowed campaign graphic.",
    image: "./assets/tech/drop-printer.jpg",
    alt: "Printer banner with a bright studio background",
  },
];

const saleProducts = [
  {
    name: "ASUS TUF Gaming A15 Ryzen 7 / 16GB / 512GB SSD",
    price: "R17,999.00",
    was: "R19,499.00",
    discount: "8% off",
    image: "./assets/tech/cutout-laptop.png",
    alt: "Laptop product image on a dark background",
  },
  {
    name: "Seagate Barracuda PCIe Gen3 SSD 1TB",
    price: "R1,999.00",
    was: "R2,249.00",
    discount: "11% off",
    image: "./assets/tech/cutout-ssd.png",
    alt: "SSD product image on a light background",
  },
  {
    name: "Brother MFC-L2759DW Mono Laser Printer",
    price: "R7,999.00",
    was: "R8,699.00",
    discount: "8% off",
    image: "./assets/tech/cutout-printer.png",
    alt: "Printer product image on a light background",
  },
  {
    name: "TP-Link Archer C6 AC1200 Dual Band Router",
    price: "R1,099.00",
    was: "R1,299.00",
    discount: "15% off",
    image: "./assets/tech/cutout-router.png",
    alt: "Router product image on a light background",
  },
  {
    name: "Tapo C425 Wire-Free Security Camera",
    price: "R2,899.00",
    was: "R3,299.00",
    discount: "12% off",
    image: "./assets/tech/cutout-camera.png",
    alt: "Security camera product image on a dark background",
  },
  {
    name: "ASUS TUF Gaming A15 Ryzen 5 / 8GB / 512GB SSD",
    price: "R15,499.00",
    was: "R16,999.00",
    discount: "9% off",
    image: "./assets/tech/cutout-laptop.png",
    alt: "Laptop product image on a dark background",
  },
  {
    name: "Seagate Barracuda PCIe Gen3 SSD 500GB",
    price: "R1,299.00",
    was: "R1,449.00",
    discount: "10% off",
    image: "./assets/tech/cutout-ssd.png",
    alt: "SSD product image on a light background",
  },
  {
    name: "Brother Compact Business Laser Printer",
    price: "R6,999.00",
    was: "R7,699.00",
    discount: "9% off",
    image: "./assets/tech/cutout-printer.png",
    alt: "Printer product image on a light background",
  },
];

const reasons = [
  {
    title: "Order Collection",
    meta: "Free pick-up point",
    copy: "Collect qualifying orders from the pick-up point when it suits your schedule.",
  },
  {
    title: "Payment Process",
    meta: "Return money within 15 days",
    copy: "Customer care helps with the payment process and return-related follow-through.",
  },
  {
    title: "24/7 Support",
    meta: "WhatsApp 073 572 1090",
    copy: "Reach the team directly on WhatsApp for product questions and order support.",
  },
  {
    title: "Customer Service",
    meta: "info@crmtechnology.co.za",
    copy: "Phone, email, and direct contact are available for sales, follow-ups, and help.",
  },
];

const categories = [
  {
    name: "Laptops",
    image: "./assets/tech/drop-laptop.jpg",
    alt: "Laptop product image on a dark background",
    shape: "shape-wide",
  },
  {
    name: "Storage",
    image: "./assets/tech/cutout-ssd.png",
    alt: "SSD product image on a light background",
    shape: "shape-tall",
  },
  {
    name: "Printers",
    image: "./assets/tech/drop-printer.jpg",
    alt: "Printer product image on a light background",
    shape: "shape-wide",
  },
  {
    name: "Security",
    image: "./assets/tech/cutout-camera.png",
    alt: "Security camera product image on a dark background",
    shape: "shape-tall",
  },
  {
    name: "Routers",
    image: "./assets/tech/drop-router.jpg",
    alt: "Router product image on a light background",
    shape: "shape-wide",
  },
  {
    name: "Gaming",
    image: "./assets/tech/hero-main.jpg",
    alt: "Laptop product image on a dark background",
    shape: "shape-tall",
  },
  {
    name: "Office Tech",
    image: "./assets/tech/deal-banner.jpg",
    alt: "Printer product image on a light background",
    shape: "shape-wide",
  },
];

const grails = [
  {
    name: "ASUS TUF Gaming A15 15.6\" Laptop",
    price: "R17,999.00",
    was: "R19,499.00",
    discount: "8% off",
    image: "./assets/tech/cutout-laptop.png",
    alt: "Laptop product image on a dark background",
  },
  {
    name: "TP-Link Archer C6 AC1200 Dual Band Router",
    price: "R1,099.00",
    was: "R1,299.00",
    discount: "15% off",
    image: "./assets/tech/cutout-router.png",
    alt: "Router product image on a light background",
  },
  {
    name: "Seagate Barracuda PCIe Gen3 SSD 1TB",
    price: "R1,999.00",
    was: "R2,249.00",
    discount: "11% off",
    image: "./assets/tech/cutout-ssd.png",
    alt: "SSD product image on a light background",
  },
  {
    name: "Tapo C425 Wire-Free Security Camera",
    price: "R2,899.00",
    was: "R3,299.00",
    discount: "12% off",
    image: "./assets/tech/cutout-camera.png",
    alt: "Security camera product image on a dark background",
  },
  {
    name: "Brother MFC-L2759DW Mono Laser Printer",
    price: "R7,999.00",
    was: "R8,699.00",
    discount: "8% off",
    image: "./assets/tech/cutout-printer.png",
    alt: "Printer product image on a light background",
  },
  {
    name: "ASUS TUF Gaming A15 Ryzen 5 / 8GB / 512GB SSD",
    price: "R15,499.00",
    was: "R16,999.00",
    discount: "9% off",
    image: "./assets/tech/cutout-laptop.png",
    alt: "Laptop product image on a dark background",
  },
];

const arrivals = [
  {
    name: "Seagate Barracuda PCIe Gen3 SSD 500GB",
    price: "R1,299.00",
    was: "R1,449.00",
    discount: "10% off",
    image: "./assets/tech/cutout-ssd.png",
    alt: "SSD product image on a light background",
    audience: "storage",
  },
  {
    name: "Seagate Barracuda PCIe Gen3 SSD 1TB",
    price: "R1,999.00",
    was: "R2,249.00",
    discount: "11% off",
    image: "./assets/tech/cutout-ssd.png",
    alt: "SSD product image on a light background",
    audience: "storage",
  },
  {
    name: "Seagate Barracuda PCIe Gen3 SSD 2TB",
    price: "R3,499.00",
    was: "R3,799.00",
    discount: "8% off",
    image: "./assets/tech/cutout-ssd.png",
    alt: "SSD product image on a light background",
    audience: "storage",
  },
  {
    name: "Seagate Barracuda PCIe Gen3 SSD 240GB",
    price: "R899.00",
    was: "R999.00",
    discount: "10% off",
    image: "./assets/tech/cutout-ssd.png",
    alt: "SSD product image on a light background",
    audience: "storage",
  },
  {
    name: "TP-Link Archer C6 AC1200 Dual Band Router",
    price: "R1,099.00",
    was: "R1,299.00",
    discount: "15% off",
    image: "./assets/tech/cutout-router.png",
    alt: "Router product image on a light background",
    audience: "security",
  },
  {
    name: "TP-Link Archer C6 Mesh-Ready Router",
    price: "R1,299.00",
    was: "R1,499.00",
    discount: "13% off",
    image: "./assets/tech/cutout-router.png",
    alt: "Router product image on a light background",
    audience: "security",
  },
  {
    name: "Tapo C425 Wire-Free Security Camera",
    price: "R2,899.00",
    was: "R3,299.00",
    discount: "12% off",
    image: "./assets/tech/cutout-camera.png",
    alt: "Security camera product image on a dark background",
    audience: "security",
  },
  {
    name: "Tapo C425 Solar Charging Kit",
    price: "R3,399.00",
    was: "R3,699.00",
    discount: "8% off",
    image: "./assets/tech/cutout-camera.png",
    alt: "Security camera product image on a dark background",
    audience: "security",
  },
];

const socialItems = [
  {
    type: "quote",
    theme: "",
    quote:
      '"Order collection is available from the free pick-up point for qualifying purchases."',
    author: "Order Collection",
  },
  {
    type: "image",
    image: "./assets/tech/about-banner.jpg",
    alt: "Laptop product image on a dark background",
    label: "About us",
    href: "https://crmtechnology.co.za/",
    classes: "row-span-2",
  },
  {
    type: "quote",
    theme: "alt",
    quote:
      '"Customer service is available on 087 265 8348 for orders, support, and follow-ups."',
    author: "Customer Service",
  },
  {
    type: "image",
    image: "./assets/tech/hero-main.jpg",
    alt: "Router product image on a light background",
    label: "Contact us",
    href: "mailto:info@crmtechnology.co.za",
    classes: "row-span-2",
  },
  {
    type: "image",
    image: "./assets/tech/deal-banner.jpg",
    alt: "Security camera product image on a dark background",
    label: "WhatsApp",
    href: "https://wa.me/27735721090",
    classes: "",
  },
  {
    type: "quote",
    theme: "alt",
    quote:
      '"Return money within 15 days is highlighted as part of the payment process support."',
    author: "Payment Process",
  },
  {
    type: "image",
    image: "./assets/tech/subscribe-banner.jpg",
    alt: "Printer product image on a light background",
    label: "Delivery",
    href: "https://crmtechnology.co.za/",
    classes: "",
  },
  {
    type: "quote",
    theme: "",
    quote:
      '"Join our list for the latest specials and stay updated without spam."',
    author: "Subscribe",
  },
];

const stores = [
  {
    city: "Customer Service",
    area: "Phone, WhatsApp & Email",
    hours: "087 265 8348 | 073 572 1090 | info@crmtechnology.co.za",
    image: "./assets/tech/about-banner.jpg",
    alt: "Router banner artwork",
  },
  {
    city: "Resources",
    area: "Policies & Delivery",
    hours: "About Us | Terms of Service | Return & Refund | Delivery",
    image: "./assets/tech/deal-banner.jpg",
    alt: "Printer banner artwork",
  },
];

const arrivalFilters = [
  { label: "All", value: "all" },
  { label: "Storage", value: "storage" },
  { label: "Security", value: "security" },
];

const THEME_STORAGE_KEY = "crmtechnology-theme";
const themeIcons = {
  dark: `
    <svg viewBox="0 0 24 24" aria-hidden="true">
      <circle cx="12" cy="12" r="4.5"></circle>
      <path d="M12 2.5v2.5"></path>
      <path d="M12 19v2.5"></path>
      <path d="M4.9 4.9l1.8 1.8"></path>
      <path d="M17.3 17.3l1.8 1.8"></path>
      <path d="M2.5 12H5"></path>
      <path d="M19 12h2.5"></path>
      <path d="M4.9 19.1l1.8-1.8"></path>
      <path d="M17.3 6.7l1.8-1.8"></path>
    </svg>
  `,
  light: `
    <svg viewBox="0 0 24 24" aria-hidden="true">
      <path d="M20 14.5A7.5 7.5 0 1 1 9.5 4 6.2 6.2 0 0 0 20 14.5Z"></path>
    </svg>
  `,
};

const state = {
  arrivalFilter: "all",
  wishlist: [],
  cart: [],
};

function renderHeroRail() {
  const root = document.querySelector("#heroRail");
  root.innerHTML = heroRail
    .map(
      (item) => `
        <article class="hero-rail-card" aria-label="${item.name}">
          <div class="hover-scale-wrap"><img src="${item.image}" alt="${item.alt}" /></div>
        </article>
      `,
    )
    .join("");
}

function renderDrops() {
  const root = document.querySelector("#dropGrid");
  root.innerHTML = drops
    .map(
      (item) => `
        <article class="drop-card">
          <div class="hover-scale-wrap bg-img-wrap"><img src="${item.image}" alt="${item.alt}" loading="lazy" /></div>
          <div class="drop-copy">
            <span class="drop-label">${item.label}</span>
            <h3>${item.title}</h3>
            <p>${item.copy}</p>
          </div>
        </article>
      `,
    )
    .join("");
}

function renderProductCard(item) {
  const priceDetail = item.was
    ? `<span>${item.was}</span>`
    : `<span aria-hidden="true"></span>`;

  const discount = item.discount
    ? `<span class="product-card-discount">${item.discount}</span>`
    : "";

  return `
    <article class="product-card" data-product="${item.name}">
      <div class="product-card-media hover-scale-wrap">
        <span class="product-card-favorite" aria-hidden="true">♡</span>
        ${discount}
        <img src="${item.image}" alt="${item.alt}" loading="lazy" />
      </div>
      <div class="product-card-body">
        <h3>${item.name}</h3>
        <div class="product-card-price-row">
          <div class="price-stack">
            <strong>${item.price}</strong>
            ${priceDetail}
          </div>
          <div class="product-card-actions">
            <button class="chip-button" type="button" data-wishlist="${item.name}">Wishlist</button>
            <button class="chip-button accent" type="button" data-cart="${item.name}">Cart</button>
          </div>
        </div>
      </div>
    </article>
  `;
}

function renderProductGrid(rootId, items) {
  const root = document.querySelector(rootId);
  root.innerHTML = items.map(renderProductCard).join("");
  bindProductActions();
}

function renderWhy() {
  const root = document.querySelector("#whyGrid");
  root.innerHTML = reasons
    .map(
      (item) => `
        <article class="why-card">
          <h3>${item.title}</h3>
          <strong>${item.meta}</strong>
          <p>${item.copy}</p>
        </article>
      `,
    )
    .join("");
}

function renderCategories() {
  const root = document.querySelector("#categoryGrid");
  root.innerHTML = categories
    .map(
      (item) => `
        <article class="category-card ${item.shape}">
          <div class="hover-scale-wrap bg-img-wrap"><img src="${item.image}" alt="${item.alt}" loading="lazy" /></div>
          <div class="category-copy">${item.name}</div>
        </article>
      `,
    )
    .join("");
}

function getVisibleArrivals() {
  if (state.arrivalFilter === "all") {
    return arrivals;
  }

  return arrivals.filter((item) => item.audience === state.arrivalFilter);
}

function renderArrivalTabs() {
  const root = document.querySelector("#arrivalTabs");
  root.innerHTML = arrivalFilters
    .map(
      (item) => `
        <button
          class="tab-button ${item.value === state.arrivalFilter ? "is-active" : ""}"
          type="button"
          data-filter="${item.value}"
        >
          ${item.label}
        </button>
      `,
    )
    .join("");

  root.querySelectorAll("[data-filter]").forEach((button) => {
    button.addEventListener("click", () => {
      state.arrivalFilter = button.dataset.filter;
      renderArrivalTabs();
      renderArrivals();
      bindProductActions();
    });
  });
}

function renderArrivals() {
  renderProductGrid("#arrivalGrid", getVisibleArrivals());
}

function renderSocial() {
  const root = document.querySelector("#socialGrid");
  root.innerHTML = socialItems
    .map((item) => {
      if (item.type === "quote") {
        return `
          <article class="social-card quote ${item.theme}">
            <blockquote>${item.quote}</blockquote>
            <cite>${item.author}</cite>
          </article>
        `;
      }

      return `
        <article class="social-card image ${item.classes}">
          <div class="hover-scale-wrap bg-img-wrap"><img src="${item.image}" alt="${item.alt}" loading="lazy" /></div>
          <a
            class="social-card-image-link"
            href="${item.href}"
            target="_blank"
            rel="noreferrer"
          >
            ${item.label}
          </a>
        </article>
      `;
    })
    .join("");
}

function renderStores() {
  const root = document.querySelector("#storeGrid");
  root.innerHTML = stores
    .map(
      (item) => `
        <article class="store-card">
          <div class="store-card-media hover-scale-wrap bg-img-wrap">
            <img src="${item.image}" alt="${item.alt}" loading="lazy" />
          </div>
          <div class="store-card-copy">
            <div>
              <h3>${item.city}</h3>
              <em>${item.area}</em>
            </div>
            <p>${item.hours}</p>
          </div>
        </article>
      `,
    )
    .join("");
}

function getAllProducts() {
  return [...saleProducts, ...grails, ...arrivals];
}

function getProductByName(name) {
  return getAllProducts().find((item) => item.name === name);
}

function bindProductActions() {
  document.querySelectorAll("[data-wishlist]").forEach((button) => {
    button.addEventListener("click", (event) => {
      event.stopPropagation();
      const product = getProductByName(button.dataset.wishlist);
      if (!product) {
        return;
      }

      if (!state.wishlist.some((item) => item.name === product.name)) {
        state.wishlist.push(product);
      }
      renderWishlistPanel();
      openPanel("wishlistPanel");
    });
  });

  document.querySelectorAll("[data-cart]").forEach((button) => {
    button.addEventListener("click", (event) => {
      event.stopPropagation();
      const product = getProductByName(button.dataset.cart);
      if (!product) {
        return;
      }
      state.cart.push(product);
      renderCartPanel();
      openPanel("cartPanel");
    });
  });

  document.querySelectorAll(".product-card").forEach((card) => {
    card.addEventListener("click", () => {
      const product = getProductByName(card.dataset.product);
      if (product) {
        openProductDialog(product);
      }
    });
  });
}

function renderWishlistPanel() {
  const root = document.querySelector("#wishlistItems");
  if (!root) {
    return;
  }

  if (!state.wishlist.length) {
    root.innerHTML = `<p class="quick-view-meta">No items in your wishlist yet.</p>`;
    return;
  }

  root.innerHTML = state.wishlist
    .map(
      (item) => `
      <article class="panel-item">
        <img src="${item.image}" alt="${item.alt}" />
        <div>
          <h3>${item.name}</h3>
          <p>${item.price}</p>
        </div>
      </article>
    `,
    )
    .join("");
}

function renderCartPanel() {
  const root = document.querySelector("#cartItems");
  if (!root) {
    return;
  }

  if (!state.cart.length) {
    root.innerHTML = `<p class="quick-view-meta">No items in your cart yet.</p>`;
    return;
  }

  const total = state.cart.reduce((sum, item) => {
    const numeric = Number(item.price.replace(/[^\d.-]/g, "")) || 0;
    return sum + numeric;
  }, 0);

  root.innerHTML =
    state.cart
      .map(
        (item) => `
      <article class="panel-item">
        <img src="${item.image}" alt="${item.alt}" />
        <div>
          <h3>${item.name}</h3>
          <p>${item.price}</p>
        </div>
      </article>
    `,
      )
      .join("") +
    `
      <div class="panel-summary">
        <span>Total</span>
        <strong>R${total.toLocaleString("en-ZA", { minimumFractionDigits: 2, maximumFractionDigits: 2 })}</strong>
      </div>
    `;
}

function openPanel(panelId) {
  const panel = document.getElementById(panelId);
  if (panel) {
    panel.classList.add("is-open");
    panel.setAttribute("aria-hidden", "false");
  }
}

function closePanels() {
  document.querySelectorAll(".side-panel").forEach((panel) => {
    panel.classList.remove("is-open");
    panel.setAttribute("aria-hidden", "true");
  });
}

function bindPanels() {
  const wishlistButton = document.querySelector("#wishlistButton");
  const cartButton = document.querySelector("#cartButton");

  wishlistButton?.addEventListener("click", () => {
    renderWishlistPanel();
    openPanel("wishlistPanel");
  });

  cartButton?.addEventListener("click", () => {
    renderCartPanel();
    openPanel("cartPanel");
  });

  document.querySelectorAll("[data-close-panel]").forEach((button) => {
    button.addEventListener("click", closePanels);
  });
}

function openProductDialog(product) {
  const dialog = document.querySelector("#productDialog");
  const content = document.querySelector("#productDialogContent");
  if (!dialog || !content) {
    return;
  }

  content.innerHTML = `
    <div class="quick-view-media">
      <img src="${product.image}" alt="${product.alt}" />
    </div>
    <div class="quick-view-body">
      <span class="quick-view-meta">Product details</span>
      <h3>${product.name}</h3>
      <div class="quick-view-price">
        <strong>${product.price}</strong>
        ${product.was ? `<span>${product.was}</span>` : ""}
      </div>
      <p>
        Premium product presentation inspired by RAWLINE style: bold layout, clean focus, and fast actions for wishlist and cart.
      </p>
      <div class="quick-view-actions">
        <button class="chip-button" type="button" id="dialogWishlistButton">Add to wishlist</button>
        <button class="chip-button accent" type="button" id="dialogCartButton">Add to cart</button>
        <button class="chip-button" type="button" id="dialogCloseButton">Close</button>
      </div>
    </div>
  `;

  dialog.showModal();

  content.querySelector("#dialogWishlistButton")?.addEventListener("click", () => {
    if (!state.wishlist.some((item) => item.name === product.name)) {
      state.wishlist.push(product);
    }
    renderWishlistPanel();
    dialog.close();
    openPanel("wishlistPanel");
  });

  content.querySelector("#dialogCartButton")?.addEventListener("click", () => {
    state.cart.push(product);
    renderCartPanel();
    dialog.close();
    openPanel("cartPanel");
  });

  content.querySelector("#dialogCloseButton")?.addEventListener("click", () => {
    dialog.close();
  });
}

function bindDialogClose() {
  const dialog = document.querySelector("#productDialog");
  if (!dialog) {
    return;
  }
  dialog.addEventListener("click", (event) => {
    const rect = dialog.getBoundingClientRect();
    const inDialog =
      event.clientX >= rect.left &&
      event.clientX <= rect.right &&
      event.clientY >= rect.top &&
      event.clientY <= rect.bottom;
    if (!inDialog) {
      dialog.close();
    }
  });
}

function bindMotionEffects() {
  const hero = document.querySelector(".hero-main");
  const heroImage = document.querySelector(".hero-image");
  if (!hero || !heroImage) {
    return;
  }

  let rafId = null;

  hero.addEventListener("mousemove", (event) => {
    if (rafId) {
      cancelAnimationFrame(rafId);
    }

    rafId = requestAnimationFrame(() => {
      const rect = hero.getBoundingClientRect();
      const x = (event.clientX - rect.left) / rect.width - 0.5;
      const y = (event.clientY - rect.top) / rect.height - 0.5;
      heroImage.style.transform = `scale(1.05) translate(${x * 12}px, ${y * 12}px)`;
    });
  });

  hero.addEventListener("mouseleave", () => {
    if (rafId) {
      cancelAnimationFrame(rafId);
    }
    heroImage.style.transform = "";
  });
}

function bindMobileMenu() {
  const button = document.querySelector("#menuButton");
  const menu = document.querySelector("#mobileMenu");
  const mobileLinks = menu.querySelectorAll("a");

  function closeMenu() {
    menu.hidden = true;
    button.setAttribute("aria-expanded", "false");
  }

  button.addEventListener("click", () => {
    const isOpen = button.getAttribute("aria-expanded") === "true";
    button.setAttribute("aria-expanded", String(!isOpen));
    menu.hidden = isOpen;
  });

  mobileLinks.forEach((link) => {
    link.addEventListener("click", closeMenu);
  });

  window.addEventListener("resize", () => {
    if (window.innerWidth > 960) {
      closeMenu();
    }
  });
}

function readStoredTheme() {
  try {
    return localStorage.getItem(THEME_STORAGE_KEY);
  } catch {
    return null;
  }
}

function storeTheme(theme) {
  try {
    localStorage.setItem(THEME_STORAGE_KEY, theme);
  } catch {
    // Ignore storage failures and keep the current session theme.
  }
}

function setTheme(theme) {
  const normalizedTheme = theme === "light" ? "light" : "dark";
  const button = document.querySelector("#themeToggle");
  document.body.dataset.theme = normalizedTheme;

  if (button) {
    const nextMode = normalizedTheme === "dark" ? "light" : "dark";
    button.innerHTML = themeIcons[normalizedTheme];
    button.setAttribute("aria-label", `Switch to ${nextMode} mode`);
    button.setAttribute("title", `Switch to ${nextMode} mode`);
  }

  storeTheme(normalizedTheme);
}

function bindThemeToggle() {
  const button = document.querySelector("#themeToggle");
  if (!button) {
    return;
  }

  const initialTheme = readStoredTheme() || document.body.dataset.theme || "dark";
  setTheme(initialTheme);

  button.addEventListener("click", () => {
    const nextTheme = document.body.dataset.theme === "dark" ? "light" : "dark";
    setTheme(nextTheme);
  });
}

function bindReveal() {
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("is-visible");
          observer.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.18 },
  );

  document.querySelectorAll(".reveal").forEach((element) => observer.observe(element));
}

function init() {
  renderHeroRail();
  renderDrops();
  renderProductGrid("#saleGrid", saleProducts);
  renderWhy();
  renderCategories();
  renderProductGrid("#grailsGrid", grails);
  renderArrivalTabs();
  renderArrivals();
  renderSocial();
  renderStores();
  renderWishlistPanel();
  renderCartPanel();
  bindMobileMenu();
  bindThemeToggle();
  bindReveal();
  bindPanels();
  bindDialogClose();
  bindMotionEffects();
  bindProductActions();
}

init();
